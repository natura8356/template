/*
* excel-reader package 추가
* 1. ${workspace.dir}/packages/local 에 excel-reader package를 복사해서 넣는다.
*     np 프로젝트에 상동 주소에 있는 package를 복사하여 사용할 것
* 2. ${workspace.dir}/APPS/${app.dir}/app.json 파일에 아래와 같이 "excel-reader"라는 항목 추가
* 3. sencha cmd를 이용하여, ex) sencha app refresh, 혹은 sencha app watch 등을 이용하여
*    변경된 설정을 적용한다
*/
{
  /**
   * The list of required packages (with optional versions; default is "latest").
   *
   * For example,
   *
   *      "requires": [
   *          "charts"
   *      ]
   *
   */
  "requires": [
    //.....
    "excel-reader"
  ]
}
